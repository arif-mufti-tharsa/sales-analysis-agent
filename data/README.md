# Data

Place `sales_data.csv` here (synthetic dataset, 793 transactions, April-June 2026).

**Columns:** `date`, `category` (HP / Aksesoris / Elektronik Rumah), `product_name`, `unit_price`, `quantity`, `total_amount`

> Data is synthetic and safe to commit. If you ever switch to real data, add `data/*.csv` to `.gitignore` instead.
